options:
  logging: CLOUD_LOGGING_ONLY  # Optional: If you don't need logs in a bucket

steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'shivam-100964258-service'  # Your Cloud Run service name
      - '--image'
      - 'us-central1-docker.pkg.dev/mercurial-song-435220-a6/shivam-100964258/app:f01b6cf'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
